# Playbook entry (append-only)

Use with repo-level `docs/agentic-swe/PLAYBOOK.md` (create the file and directory if missing). Each entry is **optional** and should be **human-reviewed** before merge.

```markdown
## YYYY-MM-DD — <short title> (work id: <id>)

- **What we learned**: <one paragraph>
- **Weak dimension / rubric** (if any): <e.g. test adequacy, security>
- **Next time**: <concrete habit or checklist item>
```

Keep entries short; link to PR or issue when useful.
