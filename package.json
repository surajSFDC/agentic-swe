{
  "name": "agentic-swe",
  "version": "3.0.5",
  "description": "Autonomous SWE pipeline (Claude Code–first; Cursor, Codex, OpenCode, and compatible hosts) — install as a plugin (marketplace or --plugin-dir). Per-work state uses .worklogs/ in your project.",
  "license": "MIT",
  "author": "Suraj Gupta",
  "homepage": "https://surajSFDC.github.io/agentic-swe/",
  "keywords": [
    "claude",
    "claude-code",
    "agent",
    "swe",
    "pipeline",
    "orchestration"
  ],
  "engines": {
    "node": ">=18"
  },
  "private": true,
  "files": [
    "CHANGELOG.md",
    "scripts",
    "CLAUDE.md",
    "AGENTS.md",
    "GEMINI.md",
    "commands",
    "phases",
    "agents",
    "templates",
    "references",
    "state-machine.json",
    ".claude-plugin/",
    ".cursor-plugin/",
    ".codex/",
    ".opencode/",
    "hooks/",
    "gemini-extension.json",
    "mcp-servers.json",
    ".version-bump.json"
  ],
  "devDependencies": {
    "ws": "^8.18.0"
  },
  "scripts": {
    "build:site": "npm run build --prefix site",
    "prepack": "node -e \"const fs=require('fs');const p=require('path');const r=p.join('phases');if(!fs.existsSync(r))process.exit(1)\"",
    "test": "node scripts/run-node-tests.js",
    "test:smoke": "bash test/smoke/run-smoke.sh",
    "test:llm": "node test/llm/run-llm-tests.cjs",
    "ci": "npm run verify && npm run version:check && npm run lint --prefix site && npm run build --prefix site && npm test",
    "verify": "node scripts/verify-sanity.js",
    "summarize-work": "node scripts/summarize-work.js",
    "version:check": "bash scripts/bump-version.sh check",
    "version:bump": "bash scripts/bump-version.sh bump"
  }
}
