# Subagent Catalog — Invalidate (documentation)

The default **`config.sh`** in this pack works **directly against `agents/subagents/*.md`** — there is **no** separate download cache to invalidate.

Use this doc only if you maintain a **fork** that adds caching; otherwise ignore this file.
