{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://agentic-swe.github.io/schemas/phase-prompt.schema.json",
  "title": "Phase Prompt Schema",
  "description": "Declares required sections for agentic-swe phase prompt files.",
  "type": "object",
  "required": ["required_headings"],
  "properties": {
    "required_headings": {
      "type": "array",
      "description": "H2 headings that must appear in every phase prompt.",
      "items": { "type": "string" },
      "default": [
        "Mission",
        "Common Rationalizations",
        "Red Flags"
      ]
    },
    "recommended_headings": {
      "type": "array",
      "description": "H2 headings that should appear but are not strictly required.",
      "items": { "type": "string" },
      "default": [
        "Procedure",
        "Inputs",
        "Required Output",
        "Failure Protocol"
      ]
    },
    "exempt_files": {
      "type": "array",
      "description": "Phase files exempt from the schema (utility phases, not pipeline states).",
      "items": { "type": "string" },
      "default": ["subagent-selection.md"]
    }
  }
}
